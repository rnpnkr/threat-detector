# Model-specific dependencies for CCTV_GUN (MMDetection)
# Assumes PyTorch, torchvision, torchaudio, and mmcv-full are installed separately
# (e.g., via conda/mim) to ensure GPU compatibility.

addict==2.4.0
numpy==1.23.4
opencv-python==4.6.0.66
openmim==0.3.2
pycocotools==2.0.5
pyyaml==6.0
terminaltables==3.1.10
tqdm==4.64.1
yapf==0.32.0
mmcls==0.25.0
Pillow==9.3.0
matplotlib==3.6.2 # Often used by mmdet visualization tools

# Note: mmdet itself is installed via 'pip install -e .' in the deployment steps. 